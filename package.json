{
  "name": "english-course",
  "private": true,
  "scripts": {
    "dev": "live-server app --port=$npm_package_config_port --no-browser",
    "dev:open": "live-server app --port=$npm_package_config_port"
  },
  "config": {
    "port": "8080"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
